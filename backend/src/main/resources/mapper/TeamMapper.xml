<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiu.qiuxun.mapper.TeamMapper">

    <select id="queryMembers" resultType="com.qiu.qiuxun.model.vo.UserVO">
        SELECT u.id, u.user_account, u.username, u.avatar_url, u.gender, u.phone, u.email,
               u.profile, u.tags, u.user_role, u.user_status, u.create_time
        FROM user_team ut LEFT JOIN user u ON ut.user_id=u.id WHERE ut.team_id=#{teamId} AND u.deleted=0 AND ut.deleted=0
    </select>

</mapper>
